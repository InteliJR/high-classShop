/**
 * Script de seed desativado
 *
 * Os dados de seed antigos dependiam de mocks em `src/mocks`, que foram
 * removidos por serem apenas de teste. Este arquivo é mantido apenas para
 * evitar erros de compilação e pode ser reimplementado depois com uma
 * estratégia de seed definitiva, se necessário.
 */
export {};
